[pytest]
pythonpath = .
testpaths = tests
addopts = -q --strict-markers
markers =
    slow: builds PDF fixtures; still runs by default
    llm: needs a live Gemini key; skipped unless LOSSLIFT_RUN_LLM_TESTS=1
